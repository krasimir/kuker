* High cpu (probably on pages that has no supported framework)
* Vue icon on the events
* When working together with VueDevtools we see an error message because of Object.create
* clear the pin for a upcoming events. The pin stays even tho there is a new event
* keep the filters when we clear
* build for firefox